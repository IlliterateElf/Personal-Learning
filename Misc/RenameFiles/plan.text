rename all files matching a criteria
    1. specify criteria
    2. get list of all files (os.walk)
    3. check each file name
        1. see if it matches criteria
            1. if matches, rename file name
        2. move to next file
    4. terminate after checking each file in the list of files
